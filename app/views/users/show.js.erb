$('#closeButton').click();
$('#navbar_actions').html("<%= escape_javascript(render "navbar_actions") %>")
$('#page').html("<%= escape_javascript(render "profile") %>")
window.history.pushState(""," ", "<%= escape_javascript(user_path(@user||current_user)) %>");